# OpenChronicle Utilities - Future Implementation

The comprehensive utilities framework has been temporarily archived until the importer modules are implemented.

## Planned Utilities

- **Chatbot Importer**: Import chatbot conversation data into OpenChronicle format
- **Assistant Importer**: Import AI assistant conversation data into OpenChronicle format
- **Storypack Importer**: Import storypack files (.zip, .json, .tar.gz) into OpenChronicle format

## Implementation Status

🚧 **Under Development** - The utility framework exists but importers are not yet implemented.

## Restored Framework

The complete utilities framework is available in `.copilot/archive/unimplemented/utilities_main.py` and can be restored when the importers are ready.

## Current Alternative

Use the existing storypack import functionality in `utilities/storypack_import/` for legacy storypack importing needs.
