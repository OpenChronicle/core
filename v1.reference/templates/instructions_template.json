{
  "template_info": {
    "name": "OpenChronicle Instructions Template",
    "description": "Story guidelines and narrative instructions for consistent storytelling",
    "version": "1.0",
    "purpose": "Provides narrative guidelines, content rules, and character voice instructions",
    "required_fields": ["project_name", "narrative_guidelines.overall_tone"],
    "optional_fields": "Any field marked with '_optional': true can be safely deleted",
    "usage": "Delete entire fields/sections with '_optional': true, or just the '_optional' key to make field required",
    "import_adaptability": {
      "arrays_expandable": "All arrays can contain any number of entries - template shows minimum structure",
      "flexible_structure": "Import processes should expand arrays and adapt field counts dynamically"
    },
    "application_behavior": {
      "empty_optional_fields": "Application will automatically ignore optional fields that are empty, null, or contain only placeholder values",
      "unused_optional_fields": "Optional fields left as {{PLACEHOLDER}} values will be treated as unused and ignored",
      "processing_logic": "Only populated optional fields with actual content will be processed by the application"
    }
  },
  "project_name": "{{PROJECT_NAME}}",
  "narrative_guidelines": {
    "overall_tone": "{{SERIOUS_LIGHT_DARK_COMEDIC}}",
    "core_themes": {
      "_optional": true,
      "_comment": "Array can expand - add as many themes as needed",
      "value": [
        "{{THEME}}",
        "{{THEME}}"
      ]
    },
    "story_format": {
      "_optional": true,
      "value": "{{FIRST_PERSON_THIRD_PERSON_OMNISCIENT}}"
    }
  },
  "content_guidelines": {
    "_optional": true,
    "content_rating": {
      "_optional": true,
      "value": "{{GENERAL_TEEN_MATURE}}"
    },
    "content_rules": {
      "_optional": true,
      "_comment": "Array can expand - add as many rules as needed",
      "value": [
        "{{RULE}}",
        "{{RULE}}"
      ]
    },
    "boundaries": {
      "_optional": true,
      "value": "{{WHAT_TO_AVOID_OR_LIMIT}}"
    }
  },
  "character_voice": {
    "_optional": true,
    "voice_guidelines": {
      "_optional": true,
      "_comment": "Array can expand - add guidelines for as many characters as needed",
      "value": [
        {
          "character_name": "{{CHARACTER_NAME}}",
          "speech_patterns": "{{HOW_THEY_TALK}}",
          "personality_notes": {
            "_optional": true,
            "value": "{{PERSONALITY_REMINDERS}}"
          }
        }
      ]
    },
    "general_voice_rules": {
      "_optional": true,
      "_comment": "Array can expand - add as many voice rules as needed",
      "value": [
        "{{VOICE_RULE}}",
        "{{VOICE_RULE}}"
      ]
    }
  }
}
