{
  "template_info": {
    "name": "OpenChronicle Scene Template",
    "description": "Simple scene tracking for story organization and continuity",
    "version": "1.0",
    "purpose": "Provides clean scene logging with essential metadata and optional details",
    "required_fields": ["scene_title", "scene_summary", "basic_info.location"],
    "optional_fields": "Any field marked with '_optional': true can be safely deleted",
    "usage": "Delete entire fields/sections with '_optional': true, or just the '_optional' key to make field required",
    "import_adaptability": {
      "arrays_expandable": "All arrays can contain any number of entries - template shows minimum structure",
      "flexible_structure": "Import processes should expand arrays and adapt field counts dynamically"
    },
    "application_behavior": {
      "empty_optional_fields": "Application will automatically ignore optional fields that are empty, null, or contain only placeholder values",
      "unused_optional_fields": "Optional fields left as {{PLACEHOLDER}} values will be treated as unused and ignored",
      "processing_logic": "Only populated optional fields with actual content will be processed by the application"
    }
  },
  "scene_title": "{{SCENE_TITLE}}",
  "scene_summary": "{{ONE_PARAGRAPH_OVERVIEW}}",
  "basic_info": {
    "scene_id": {
      "_optional": true,
      "value": "{{SCENE_ID}}"
    },
    "date": {
      "_optional": true,
      "value": "{{YYYY-MM-DD}}"
    },
    "time": {
      "_optional": true,
      "value": "{{HH:MM}}"
    },
    "location": "{{CANONICAL_LOCATION}}",
    "scene_type": {
      "_optional": true,
      "value": "{{ACTION_DIALOGUE_EXPOSITION_TRANSITION}}"
    }
  },
  "participants": {
    "_optional": true,
    "characters_present": {
      "_optional": true,
      "_comment": "Array can expand - add as many characters as needed",
      "value": [
        "{{CHARACTER}}",
        "{{CHARACTER}}"
      ]
    },
    "point_of_view": {
      "_optional": true,
      "value": "{{POV_CHARACTER}}"
    },
    "key_interactions": {
      "_optional": true,
      "_comment": "Array can expand - add as many interactions as needed",
      "value": [
        "{{INTERACTION}}",
        "{{INTERACTION}}"
      ]
    }
  },
  "story_elements": {
    "_optional": true,
    "emotional_tone": {
      "_optional": true,
      "value": "{{TENSE_PEACEFUL_DRAMATIC_COMEDIC}}"
    },
    "plot_significance": {
      "_optional": true,
      "value": "{{WHY_THIS_SCENE_MATTERS}}"
    },
    "scene_tags": {
      "_optional": true,
      "_comment": "Array can expand - add as many tags as needed",
      "value": [
        "{{TAG}}",
        "{{TAG}}"
      ]
    }
  }
}
