{
  "template_info": {
    "name": "OpenChronicle Location Template",
    "description": "Simple location identification for specific places within your world",
    "version": "1.0",
    "purpose": "Provides clean location tracking for taverns, shops, rooms, and scene-specific places",
    "required_fields": ["location_name", "location_type"],
    "optional_fields": "Any field marked with '_optional': true can be safely deleted",
    "usage": "Delete entire fields/sections with '_optional': true, or just the '_optional' key to make field required",
    "import_adaptability": {
      "arrays_expandable": "All arrays can contain any number of entries - template shows minimum structure",
      "flexible_structure": "Import processes should expand arrays and adapt field counts dynamically"
    },
    "application_behavior": {
      "empty_optional_fields": "Application will automatically ignore optional fields that are empty, null, or contain only placeholder values",
      "unused_optional_fields": "Optional fields left as {{PLACEHOLDER}} values will be treated as unused and ignored",
      "processing_logic": "Only populated optional fields with actual content will be processed by the application"
    }
  },
  "location_name": "{{LOCATION_NAME}}",
  "location_type": "{{TAVERN_SHOP_ROOM_STREET_BUILDING}}",
  "basic_info": {
    "description": "{{BRIEF_DESCRIPTION}}",
    "primary_atmosphere": "{{COZY_BUSTLING_QUIET_MYSTERIOUS}}",
    "notable_features": {
      "_optional": true,
      "_comment": "Array can expand - add as many features as needed",
      "value": [
        "{{FEATURE}}",
        "{{FEATURE}}"
      ]
    }
  },
  "scene_details": {
    "_optional": true,
    "typical_occupants": {
      "_optional": true,
      "_comment": "Array can expand - add as many occupants as needed",
      "value": [
        "{{PERSON}}",
        "{{PERSON}}"
      ]
    },
    "story_significance": {
      "_optional": true,
      "value": "{{WHY_IT_MATTERS}}"
    },
    "access_notes": {
      "_optional": true,
      "value": "{{HOW_TO_GET_THERE}}"
    }
  },
  "reference_tags": {
    "_optional": true,
    "location_tags": {
      "_optional": true,
      "_comment": "Array can expand - add as many tags as needed",
      "value": [
        "{{TAG}}",
        "{{TAG}}"
      ]
    },
    "connected_locations": {
      "_optional": true,
      "_comment": "Array can expand - add as many connections as needed",
      "value": [
        "{{NEARBY_LOCATION}}",
        "{{NEARBY_LOCATION}}"
      ]
    }
  }
}
