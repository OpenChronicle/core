{
  "template_info": {
    "name": "OpenChronicle Style Guide Template",
    "description": "Writing style and narrative voice guidelines for consistent storytelling",
    "version": "1.0",
    "purpose": "Provides narrative style, dialogue patterns, and writing consistency guidelines",
    "required_fields": ["story_title", "narrative_style.perspective", "narrative_style.tone"],
    "optional_fields": "Any field marked with '_optional': true can be safely deleted",
    "usage": "Delete entire fields/sections with '_optional': true, or just the '_optional' key to make field required",
    "import_adaptability": {
      "arrays_expandable": "All arrays can contain any number of entries - template shows minimum structure",
      "flexible_structure": "Import processes should expand arrays and adapt field counts dynamically"
    },
    "application_behavior": {
      "empty_optional_fields": "Application will automatically ignore optional fields that are empty, null, or contain only placeholder values",
      "unused_optional_fields": "Optional fields left as {{PLACEHOLDER}} values will be treated as unused and ignored",
      "processing_logic": "Only populated optional fields with actual content will be processed by the application"
    }
  },
  "story_title": "{{STORY_TITLE}}",
  "narrative_style": {
    "perspective": "{{FIRST_PERSON_THIRD_PERSON_OMNISCIENT}}",
    "tone": "{{SERIOUS_LIGHT_DRAMATIC_COMEDIC}}",
    "tense": {
      "_optional": true,
      "value": "{{PAST_PRESENT_FUTURE}}"
    },
    "description_style": {
      "_optional": true,
      "value": "{{DETAILED_SPARSE_ATMOSPHERIC}}"
    }
  },
  "dialogue_style": {
    "_optional": true,
    "general_guidelines": {
      "_optional": true,
      "_comment": "Array can expand - add as many guidelines as needed",
      "value": [
        "{{GUIDELINE}}",
        "{{GUIDELINE}}"
      ]
    },
    "character_voices": {
      "_optional": true,
      "_comment": "Array can expand - add voice guides for as many characters as needed",
      "value": [
        {
          "character_name": "{{CHARACTER_NAME}}",
          "speech_patterns": {
            "_optional": true,
            "_comment": "Array can expand - add as many patterns as needed",
            "value": [
              "{{PATTERN}}",
              "{{PATTERN}}"
            ]
          },
          "voice_notes": {
            "_optional": true,
            "value": "{{HOW_THEY_SPEAK}}"
          }
        }
      ]
    }
  },
  "writing_guidelines": {
    "_optional": true,
    "style_rules": {
      "_optional": true,
      "_comment": "Array can expand - add as many style rules as needed",
      "value": [
        "{{RULE}}",
        "{{RULE}}"
      ]
    },
    "word_choices": {
      "_optional": true,
      "value": "{{VOCABULARY_PREFERENCES}}"
    },
    "consistency_notes": {
      "_optional": true,
      "_comment": "Array can expand - add as many consistency notes as needed",
      "value": [
        "{{NOTE}}",
        "{{NOTE}}"
      ]
    }
  }
}
