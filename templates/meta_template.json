{
  "template_info": {
    "name": "OpenChronicle Meta Template",
    "description": "Essential story metadata and configuration settings",
    "version": "1.0",
    "purpose": "Provides story identification and basic configuration management",
    "required_fields": ["story_name", "title", "author", "basic_settings.genre"],
    "optional_fields": "Any field marked with '_optional': true can be safely deleted",
    "usage": "Delete entire fields/sections with '_optional': true, or just the '_optional' key to make field required",
    "import_adaptability": {
      "arrays_expandable": "All arrays can contain any number of entries - template shows minimum structure",
      "flexible_structure": "Import processes should expand arrays and adapt field counts dynamically"
    },
    "application_behavior": {
      "empty_optional_fields": "Application will automatically ignore optional fields that are empty, null, or contain only placeholder values",
      "unused_optional_fields": "Optional fields left as {{PLACEHOLDER}} values will be treated as unused and ignored",
      "processing_logic": "Only populated optional fields with actual content will be processed by the application"
    }
  },
  "story_name": "{{STORY_NAME}}",
  "title": "{{STORY_TITLE}}",
  "author": "{{AUTHOR_NAME}}",
  "basic_settings": {
    "genre": "{{FANTASY_SCIFI_MODERN_HISTORICAL}}",
    "tone": {
      "_optional": true,
      "value": "{{DARK_LIGHT_SERIOUS_COMEDIC}}"
    },
    "target_audience": {
      "_optional": true,
      "value": "{{ADULT_YA_TEEN_GENERAL}}"
    }
  },
  "story_info": {
    "_optional": true,
    "description": {
      "_optional": true,
      "value": "{{STORY_DESCRIPTION}}"
    },
    "version": {
      "_optional": true,
      "value": "1.0.0"
    },
    "created": {
      "_optional": true,
      "value": "{{CREATION_DATE}}"
    },
    "last_modified": {
      "_optional": true,
      "value": "{{LAST_MODIFIED_DATE}}"
    }
  },
  "organization": {
    "_optional": true,
    "story_tags": {
      "_optional": true,
      "_comment": "Array can expand - add as many tags as needed",
      "value": [
        "{{TAG}}",
        "{{TAG}}"
      ]
    },
    "content_warnings": {
      "_optional": true,
      "_comment": "Array can expand - add as many warnings as needed",
      "value": [
        "{{WARNING}}",
        "{{WARNING}}"
      ]
    },
    "chapter_structure": {
      "_optional": true,
      "value": "{{HOW_CHAPTERS_ORGANIZED}}"
    }
  }
}
