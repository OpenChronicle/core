Please update the following files with the latest, most actionable data from the `core` directory:

1. `core\analysis\gpt\core_method_inventory.md`
   - Refresh the list of all classes, methods, and functions in each `core` module.
   - Add a header line: `Last refreshed: [YYYY-MM-DD HH:mm]`
   - For each method/function, include its signature and a one-line summary (if available from docstring or comments).
   - Group methods by class, and highlight any methods with similar names or signatures across modules (potential helper candidates).
   - Flag duplicate or highly similar methods/functions for review.
   - Note any methods that are imported from other modules or utilities.
   - At the end, provide a summary table of shared method names and their locations.

2. `core\analysis\gpt\core_refactor_priority.md`
   - Update the line count summary for each `core` file (use PowerShell for accuracy).
   - Recalculate refactoring priorities based on line count, cyclomatic complexity (if available), and number of classes/functions.
   - Add a header line: `Last refreshed: [YYYY-MM-DD HH:mm]`
   - For each file, list the top 3 largest classes/functions and their line counts.
   - Flag files with high coupling (many imports from other core modules) or low cohesion (many unrelated functions).
   - Suggest specific refactoring actions (e.g., extract utility, split class, modularize config).
   - At the end, provide a summary of files with the most urgent refactor needs and why.

Use PowerShell for line counts:
```powershell
Get-ChildItem core\*.py | ForEach-Object { $_.Name + ': ' + (Get-Content $_.FullName | Measure-Object -Line).Lines }
```

For cyclomatic complexity:
```powershell
python -m radon cc core --total-average
```

The goal: Make these files a living diagnostic dashboard for architecture and refactoring. Every refresh should help you spot duplication, complexity, and opportunities for improvement at a glance.