"""
Context Orchestrator (removed)

This module previously hosted the unified ContextOrchestrator. It has been
removed from core. Resolve an implementation of the domain port `IContextPort`
via plugins/DI (e.g., storytelling plugin).
"""

raise ImportError("ContextOrchestrator was removed from core. Use IContextPort via plugins/DI.")
