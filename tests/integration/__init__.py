"""Integration tests for OpenChronicle.

These tests require external dependencies (LLM providers, databases, etc.)
and are opt-in via environment variables.

Run with: OC_INTEGRATION_TESTS=1 pytest tests/integration/
"""
